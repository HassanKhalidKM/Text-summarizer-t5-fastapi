name: Docker CI

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t text-summarizer .

      - name: Verify app code compiles inside image
        run: docker run --rm text-summarizer python -m py_compile app.py

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set lowercase owner name
        run: echo "OWNER_LC=$(echo '${{ github.actor }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Tag and push image
        run: |
          docker tag text-summarizer ghcr.io/${{ env.OWNER_LC }}/text-summarizer:latest
          docker push ghcr.io/${{ env.OWNER_LC }}/text-summarizer:latest